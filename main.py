#将列表里的数当成二进制数组合出来换算成十进制数
pd = [1, 1, 1]    #这里是填二进制数组
c = len(pd)-1     #获取元素数减一来做二的平方数，保证最后以后一个数是乘的二的零次方
reg = 0           #每次循环的结果要相加
for r in pd:
    t = r * 2**c
    c -= 1
    reg += t
print(reg)
